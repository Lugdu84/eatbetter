const favoriteJS = () => {
const favorite = document.getElementById("favorite-id<%= favorite.id %>");
// cache le favoris détruit
if (favorite) {
    favorite.classList.add('hidden');
}

}
export { favoriteJS };
